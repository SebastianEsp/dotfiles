# No secrets are required for this server.
#
# It uses TYPE=FTBA, which downloads the modpack from the open Feed The Beast API
# (no CurseForge API key needed). This file is kept only as a reminder.
#
# A CF_API_KEY would only be needed if you switched to TYPE=AUTO_CURSEFORGE.
</content>
